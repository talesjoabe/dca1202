#include <stdio.h>

/*/
int main(){
  int vet[] = {4,9,13}; // inicializa um vetor de inteiros com os valores 4,9,13
  int i; // declara uma variavel inteira i

  for(i=0;i<3;i++){ // percorre todo o vetor de tamanho 3
    printf("%d ",*(vet+i)); // imprime cada valor do vetor (4,9,13) seguido de espaço
  }
}
/*/

/*/
int main(){
  int vet[] = {4,9,13}; // inicializa um vetor de inteiros com os valores 4,9,13
  int i; // declara uma variavel inteira i
  for(i=0;i<3;i++){// percorre todo o vetor de tamanho 3
    printf("%X ",vet+i);// imprime a posição de cada elemento do vetor
  }
}
/*/
