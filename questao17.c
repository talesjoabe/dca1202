#include <stdio.h>

int main()
{
    /*
    // utilizei os mecanismos de verificação de tempo de execução nas questões anteriores.
    // com a seguinte estrutura:

    // clock_t Ticks[2];
    // Ticks[0] = clock();
    // O código a ter seu tempo de execução medido ficaria neste ponto.
    // Ticks[1] = clock();
    // double Tempo = (Ticks[1] - Ticks[0]) * 1000.0 / CLOCKS_PER_SEC;
    // printf("Tempo gasto: %g ms.", Tempo);
    // getchar();

    // quanto ao tempo de execução. houve uma diferença considerável entre a questao 15 e 16.
    // enquanto a 15 executa em torno de 1 ms, a 16 em torno de 1000ms
    /*/
    printf("Hello World!\n");
    return 0;
}
